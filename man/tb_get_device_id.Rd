% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/push_to_thingsboard.R
\name{tb_get_device_id}
\alias{tb_get_device_id}
\title{Look Up a ThingsBoard Device's UUID by Name}
\usage{
tb_get_device_id(
  device_name,
  api_key = Sys.getenv("TB_API_KEY"),
  host = Sys.getenv("TB_HOST", unset = "https://thingsboard.cloud"),
  username = Sys.getenv("TB_USERNAME"),
  password = Sys.getenv("TB_PASSWORD")
)
}
\arguments{
\item{device_name}{device name as shown in the ThingsBoard UI.}

\item{api_key}{account-level API key. Defaults to env var \code{TB_API_KEY}.}

\item{host}{base URL of the ThingsBoard instance. Defaults to env var
\code{TB_HOST} if set, otherwise \code{"https://thingsboard.cloud"}.}

\item{username}{ThingsBoard user for the username/password (JWT) login
(self-hosted / Community Edition). Defaults to env var \code{TB_USERNAME}.}

\item{password}{ThingsBoard password. Defaults to env var \code{TB_PASSWORD}.}
}
\value{
device UUID (character) or \code{NA_character_} if the lookup did not
resolve.
}
\description{
Lightweight read-only companion to \code{\link{tb_setup_devices}}: when you
only need a device's internal UUID (e.g. to call the telemetry-delete
endpoint), this returns it directly without touching the access token or
creating the device on the side. Returns \code{NA_character_} when the device
does not exist.
}
\examples{
\dontrun{
tb_get_device_id("wasserportal-gw-6038")
}
}
