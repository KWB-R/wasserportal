% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/push_to_thingsboard.R
\name{tb_delete_device_telemetry}
\alias{tb_delete_device_telemetry}
\title{Delete All Time-Series Data for Selected Keys on a ThingsBoard Device}
\usage{
tb_delete_device_telemetry(
  device_id,
  keys = NULL,
  api_key = Sys.getenv("TB_API_KEY"),
  host = Sys.getenv("TB_HOST", unset = "https://thingsboard.cloud"),
  delete_latest = TRUE,
  username = Sys.getenv("TB_USERNAME"),
  password = Sys.getenv("TB_PASSWORD")
)
}
\arguments{
\item{device_id}{device UUID.}

\item{keys}{character vector of telemetry keys to delete, or \code{NULL} to
clear every key the device currently knows.}

\item{api_key}{account-level API key. Defaults to env var \code{TB_API_KEY}.}

\item{host}{base URL of the ThingsBoard instance. Defaults to env var
\code{TB_HOST} if set, otherwise \code{"https://thingsboard.cloud"}.}

\item{delete_latest}{if \code{TRUE} (default) ThingsBoard also drops the
cached "latest telemetry" entry so the device-detail tab in the UI
immediately reflects the deletion. Set to \code{FALSE} to keep the latest
value visible for keys that get repopulated by the next push anyway.}

\item{username}{ThingsBoard user for the username/password (JWT) login
(self-hosted / Community Edition). Defaults to env var \code{TB_USERNAME}.}

\item{password}{ThingsBoard password. Defaults to env var \code{TB_PASSWORD}.}
}
\value{
invisibly the number of keys submitted for deletion.
}
\description{
Wipes historical telemetry rows from ThingsBoard for the given device and
keys via \verb{DELETE /api/plugins/telemetry/DEVICE/\{id\}/timeseries/delete}.
Pass \code{keys = NULL} (the default) to clear every key the device currently
knows -- the function then calls
\code{\link{tb_list_device_telemetry_keys}} first to discover them.
}
\details{
Server-side attributes (latitude, longitude, Bezirk, ...) and the device
itself are NOT touched, only the time-series telemetry store. Re-running
the demo push afterwards re-fills the cleared keys with the real
Wasserportal timestamps.
}
\examples{
\dontrun{
id <- tb_get_device_id("wasserportal-gw-6038")
# wipe everything currently stored:
tb_delete_device_telemetry(id)
# wipe just the smoke-test GW-Stand value:
tb_delete_device_telemetry(id, keys = "GW-Stand")
}
}
