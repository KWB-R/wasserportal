% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/push_to_thingsboard.R
\name{tb_push_station_telemetry}
\alias{tb_push_station_telemetry}
\title{Push Time Series of one Wasserportal Station to ThingsBoard}
\usage{
tb_push_station_telemetry(
  data,
  device_token,
  ts_col = "Datum",
  value_col = "Messwert",
  key_col = "Parameter",
  single_key = "value",
  host = Sys.getenv("TB_HOST", unset = "https://thingsboard.cloud"),
  chunk_size = 100L,
  mode = c("single", "bulk"),
  throttle_seconds = NULL,
  max_active = 10L,
  verbose = TRUE
)
}
\arguments{
\item{data}{data frame for \strong{one} station, in the long format produced by
\code{\link{get_groundwater_data}} (columns \code{Messstellennummer}, \code{Datum},
\code{Parameter}, \code{Einheit}, \code{Messwert}) or
\code{\link{get_daily_surfacewater_data}} (\code{Messstellennummer}, \code{Datum},
\code{Tagesmittelwert}, \code{Parameter}, \code{Einheit}).}

\item{device_token}{ThingsBoard device access token (taken from the device
detail view in the ThingsBoard UI).}

\item{ts_col}{name of the timestamp column. Default \code{"Datum"}.}

\item{value_col}{name of the numeric value column. Default \code{"Messwert"} (set
to \code{"Tagesmittelwert"} for surface water data).}

\item{key_col}{name of the column whose values become telemetry keys.
Default \code{"Parameter"}. Set to \code{NULL} to push \code{value_col} itself under a
single fixed key (see \code{single_key}).}

\item{single_key}{telemetry key used when \code{key_col} is \code{NULL}. Default
\code{"value"}.}

\item{host}{base URL of the ThingsBoard instance, without trailing slash.
Defaults to env var \code{TB_HOST} if set, otherwise
\code{"https://thingsboard.cloud"}.}

\item{chunk_size}{maximum number of timestamps per HTTP POST when
\code{mode = "bulk"}. Default \code{100}. Ignored in single mode.}

\item{mode}{one of \code{"single"} (default) or \code{"bulk"}. The Maker free
tier on ThingsBoard Cloud rejects the bulk array form with an
opaque HTTP 500 even though the same device accepts the per-record
\verb{\{"ts": ms, "values": \{...\}\}} object; single mode therefore POSTs
each record on its own. Use \code{"bulk"} against self-hosted CE for
the much faster array-of-records form. See
\code{\link{tb_plan_defaults}} for plan-aware presets that pick
\code{mode}, \code{chunk_size} and \code{throttle_seconds} together.}

\item{throttle_seconds}{inter-request sleep, in seconds, between
consecutive HTTP POSTs. \code{NULL} (default) picks \code{0.05} for
\code{mode = "single"} and \code{0.1} for \code{mode = "bulk"}. Increase to stay
safely below the per-second / per-minute transport rate limits of
the target ThingsBoard plan; set to \code{0} to push as fast as the
server permits (e.g. self-hosted CE).}

\item{max_active}{number of concurrent HTTP POSTs in single mode
(passed to \code{httr2::req_perform_parallel()}). Default \code{10}, which
stays below the ThingsBoard Cloud Free tier's 50 messages/second
per-device transport rate limit. Ignored in bulk mode.}

\item{verbose}{print one line per chunk in bulk mode and one line
per parallel batch in single mode (default \code{TRUE}).}
}
\value{
invisibly the number of telemetry timestamps that were sent.
}
\description{
Sends long-format measurements of a single Wasserportal monitoring station
to the device telemetry endpoint of a ThingsBoard instance
(\verb{/api/v1/\{token\}/telemetry}). Works against ThingsBoard Cloud
(e.g. the free Maker tier on \verb{https://thingsboard.cloud}),
self-hosted ThingsBoard Community Edition and \verb{https://demo.thingsboard.io}
since the device-token API is identical on all of them.
}
\details{
Long-format input is pivoted on the fly so that every distinct value of
\code{key_col} becomes one telemetry key inside ThingsBoard, sharing the same
timestamp.
}
\examples{
\dontrun{
stations <- wasserportal::get_stations()
gw <- wasserportal::get_groundwater_data(stations)
one_station <- dplyr::filter(
  gw$groundwater.level,
  .data$Messstellennummer == "149"
)
tb_push_station_telemetry(
  data = one_station,
  device_token = Sys.getenv("TB_DEVICE_TOKEN_149")
)
}
}
