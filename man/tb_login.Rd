% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/push_to_thingsboard.R
\name{tb_login}
\alias{tb_login}
\title{Obtain a JWT Bearer Token from ThingsBoard (Username / Password Login)}
\usage{
tb_login(
  username = Sys.getenv("TB_USERNAME"),
  password = Sys.getenv("TB_PASSWORD"),
  host = Sys.getenv("TB_HOST", unset = "https://thingsboard.cloud")
)
}
\arguments{
\item{username}{ThingsBoard user (usually the account e-mail). Defaults to
env var \code{TB_USERNAME}.}

\item{password}{ThingsBoard password. Defaults to env var \code{TB_PASSWORD}.}

\item{host}{base URL of the ThingsBoard instance, without trailing slash.
Defaults to env var \code{TB_HOST} if set, otherwise
\code{"https://thingsboard.cloud"}. Use e.g.
\code{"https://dashboards.inowas.org"} for a self-hosted instance.}
}
\value{
the JWT access token as a character scalar, ready to be sent in an
\verb{X-Authorization: Bearer <token>} request header.
}
\description{
Calls \code{POST /api/auth/login} with a username/password pair and returns the
JWT access token. This is the \strong{standard ThingsBoard REST API
authentication} and the only one available on self-hosted ThingsBoard
Community Edition: unlike the account-level \emph{API key} (a ThingsBoard Cloud
convenience, generated under \emph{Account > Security > API keys}), every
edition -- CE, PE and Cloud -- accepts a username/password login.
}
\details{
The token is short-lived (ThingsBoard's default JWT expiration is 2.5 h),
which is ample for the one-off device setup done by
\code{\link{tb_setup_devices}}: the subsequent telemetry push uses the
per-device access token, not this JWT, so no token refresh is implemented.
}
\examples{
\dontrun{
Sys.setenv(
  TB_HOST = "https://dashboards.inowas.org",
  TB_USERNAME = "me@example.org",
  TB_PASSWORD = "secret"
)
token <- tb_login()
}
}
