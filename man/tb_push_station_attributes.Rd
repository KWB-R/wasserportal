% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/push_to_thingsboard.R
\name{tb_push_station_attributes}
\alias{tb_push_station_attributes}
\title{Push Static Attributes of one Wasserportal Station to ThingsBoard}
\usage{
tb_push_station_attributes(
  attributes,
  device_token,
  host = tb_default_host()
)
}
\arguments{
\item{attributes}{named list (or a one-row data frame, which is converted to
a list). All entries must be JSON-serialisable scalars.}

\item{device_token}{ThingsBoard device access token.}

\item{host}{base URL of the ThingsBoard instance. Defaults to env var
\code{TB_HOST} if set, otherwise \code{"https://thingsboard.cloud"}.}
}
\value{
invisibly the number of attributes that were sent.
}
\description{
Sends station metadata (coordinates, level reference, operator, ...) as
client-side attributes to the ThingsBoard device attributes endpoint
(\verb{/api/v1/\{token\}/attributes}). Attributes are key/value pairs without a
timestamp; ThingsBoard overwrites the previous value on every push.
}
\examples{
\dontrun{
tb_push_station_attributes(
  attributes = list(
    name = "Pegel Mueggelheim",
    latitude = 52.4291,
    longitude = 13.6450,
    pegelnullpunkt_m_NHN = 32.18
  ),
  device_token = Sys.getenv("TB_DEVICE_TOKEN_5867000")
)
}
}
